{
  "name": "@managed-agents/app-harness-pi-no-compaction",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --ip 127.0.0.1",
    "build": "wrangler deploy --dry-run --outdir dist",
    "check": "pnpm typecheck && pnpm test && pnpm build",
    "typecheck": "tsc -p tsconfig.json && tsc -p tsconfig.test.json",
    "test": "node --test test/*.test.ts"
  },
  "dependencies": {
    "@managed-agents/diagnostics": "workspace:*",
    "@managed-agents/harness-pi-no-compaction": "workspace:*",
    "@managed-agents/contracts": "workspace:*",
    "@managed-agents/session-runtime": "workspace:*",
    "@managed-agents/session-execution": "workspace:*"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "5.20260910.1",
    "esbuild": "0.27.3",
    "miniflare": "4.20260730.0",
    "wrangler": "4.116.0"
  }
}
